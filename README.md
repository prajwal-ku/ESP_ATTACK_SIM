# ESP Attack Sim Split Workspace

This workspace is now separated into two standalone repos:

- `dashboard-repo`: live traffic dashboard and analysis server
- `attacker-repo`: ESP8266-based DoS simulation client

Each folder is structured so it can be initialized as its own Git repository.
